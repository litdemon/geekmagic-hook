# Claude Code local settings (contains personal hook paths and permissions)
.claude/settings.local.json
**/.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# macOS
.DS_Store
._*

# Installed app files (managed by make install)
# ~/.geekmagic_hook/ is intentionally outside this repo
